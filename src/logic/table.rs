use std::sync::Arc;

enum cardState {
    placed,
    excluded,
    removed,
}

#[derive(Debug)]
struct table {
    game_state: bool,
    size: u8,
}

impl table {
    /*fn construct_the_table(&self) -> Arc<u8> {
        let table:
    }*/
}
